package com.one.browser.config;

import android.os.Environment;

/**
 * @author 18517
 */
public class AppConfig {

    /**
     * 文件保存目录
     */
    public static String PACKAGE = Environment.getExternalStoragePublicDirectory(Environment.DIRECTORY_DOWNLOADS) + "/" + "一小只";
    public static String PACK = "一小只";
}
