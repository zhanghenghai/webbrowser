<shape xmlns:android="http://schemas.android.com/apk/res/android"
        android:shape="rectangle">
        <size android:width="0dp"/>
        <corners android:topLeftRadius="30dp" android:topRightRadius="30dp"/>   <!-- 设置圆角弧度 -->
        <solid android:color="#FFF"/>   <!-- 设置背景颜色 -->
        <stroke android:color="@color/Transparent" android:width="0dp"/> <!-- 设置边框颜色以及宽度 -->
 </shape>
